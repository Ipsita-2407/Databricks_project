# The main pipeline for databricks_project_bundle

resources:
  pipelines:
    bronze_silver_earthquake_etl:
      name: Bronze_Silver_Earthquake_Prod
      configuration:
        catalog_name: ${var.catalog}
      libraries:
        - glob:
            include: ../src/DLT_pipeline/Bronze_Silver_Earthquake/transformations/**
      schema: silver
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog}
      serverless: true
      root_path: ../src/DLT_pipeline/Bronze_Silver_Earthquake


